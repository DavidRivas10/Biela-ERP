generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Phase 1 intentionally has no operational business models. Products,
// vehicles, compatibility, inventory, sales, and workshop belong to later phases.
